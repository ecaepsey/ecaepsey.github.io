---
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <title>My Training — Damir Aushenov</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

      body {
        background: #05080d url('/bg.jpeg') center center / cover no-repeat fixed;
        font-family: 'Rajdhani', sans-serif;
        min-height: 100vh;
      }

      /* ── profile header ── */
      .profile-header {
        background: linear-gradient(180deg, #0a1220 0%, #0d1520 100%);
        border-bottom: 1px solid #1a2a1a;
        padding: 16px 32px;
        display: flex;
        align-items: center;
        gap: 16px;
      }
      .profile-header img {
        width: 56px;
        height: 56px;
        object-fit: cover;
        object-position: center top;
        border: 2px solid #3a5a3a;
        display: block;
      }
      .profile-header-name {
        font-size: 22px;
        font-weight: 700;
        color: #ffffff;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        line-height: 1;
      }
      .profile-header-title {
        margin-top: 4px;
        font-size: 13px;
        font-weight: 500;
        color: #4db84d;
        letter-spacing: 0.06em;
        text-transform: uppercase;
      }

      /* ── nav ── */
      nav {
        background: linear-gradient(180deg, #0d1520 0%, #080e18 100%);
        border-bottom: 1px solid #1a2a1a;
      }
      .nav-tabs {
        display: flex;
        list-style: none;
        padding: 0 32px;
      }
      .nav-tabs li a {
        display: block;
        padding: 18px 28px 16px;
        font-size: 17px;
        font-weight: 600;
        letter-spacing: 0.04em;
        text-decoration: none;
        color: #4db84d;
        border-bottom: 3px solid transparent;
        transition: color 0.15s, border-color 0.15s;
        position: relative;
        top: 1px;
      }
      .nav-tabs li a:hover {
        color: #80e080;
        border-bottom-color: rgba(100, 220, 100, 0.35);
      }
      .nav-tabs li.active a {
        color: #8fff6a;
        border-bottom-color: #8fff6a;
      }

      /* ── page ── */
      .page {
        padding: 40px 32px;
      }

      /* ── training log ── */
      .log-block {
        width: 480px;
        background: rgba(6, 12, 24, 0.82);
        border-top: 2px solid #8fff6a;
        padding: 22px 24px 28px;
      }

      .log-block h2 {
        font-size: 26px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 4px;
      }

      .log-date {
        font-size: 13px;
        font-weight: 500;
        color: #7a8a9a;
        margin-bottom: 24px;
        letter-spacing: 0.04em;
      }

      /* ── session entry ── */
      .session {
        border-left: 2px solid #2a4a2a;
        padding-left: 16px;
        margin-bottom: 20px;
      }

      .session:last-child { margin-bottom: 0; }

      .session-type {
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: #4db84d;
        margin-bottom: 4px;
      }

      .session-title {
        font-size: 20px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 12px;
        line-height: 1;
      }

      .session-stats {
        display: flex;
        gap: 24px;
      }

      .session-stat {
        display: flex;
        flex-direction: column;
        gap: 2px;
      }

      .session-stat-label {
        font-size: 11px;
        color: #7a8a9a;
        letter-spacing: 0.04em;
      }

      .session-stat-value {
        font-size: 18px;
        font-weight: 600;
        color: #ffffff;
      }

      .session-stat-value.blue { color: #4ab4ff; }

      .divider {
        height: 1px;
        background: rgba(255,255,255,0.07);
        margin: 20px 0;
      }
    </style>
  </head>
  <body>
    <div class="profile-header">
      <img src="/avatar.jpg" alt="Damir Aushenov" />
      <div>
        <div class="profile-header-name">Damir Aushenov</div>
        <div class="profile-header-title">iOS Developer</div>
      </div>
    </div>

    <nav>
      <ul class="nav-tabs">
        <li><a href="/">Main</a></li>
        <li><a href="/works">My Works</a></li>
        <li class="active"><a href="/training">My Training</a></li>
      </ul>
    </nav>

    <div class="page">
      <div class="log-block">
        <h2>Training Log</h2>
        <div class="log-date">February 28, 2026</div>

        <!-- Session 1 -->
        <div class="session">
          <div class="session-type">🏊 Swimming</div>
          <div class="session-title">Pool Swim</div>
          <div class="session-stats">
            <div class="session-stat">
              <span class="session-stat-label">Distance</span>
              <span class="session-stat-value blue">676 m</span>
            </div>
            <div class="session-stat">
              <span class="session-stat-label">Time</span>
              <span class="session-stat-value">25 min</span>
            </div>
          </div>
        </div>

        <div class="divider"></div>

        <!-- Session 2 -->
        <div class="session">
          <div class="session-type">🚶 Walking</div>
          <div class="session-title">Indoor Walk</div>
          <div class="session-stats">
            <div class="session-stat">
              <span class="session-stat-label">Distance</span>
              <span class="session-stat-value blue">5 km</span>
            </div>
            <div class="session-stat">
              <span class="session-stat-label">Time</span>
              <span class="session-stat-value">1 hr</span>
            </div>
          </div>
        </div>

      </div>
    </div>
  </body>
</html>
